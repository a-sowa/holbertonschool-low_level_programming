#include "main.h"

/**
 * mul - Entry point
 * mul func: multiply two integers and return the value
 * @a: input integer
 * @b: input integer
 * Return: result value (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}
