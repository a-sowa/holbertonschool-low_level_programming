#include "main.h"

/**
 * _isdigit - Entry point
 * _isdigit func: Return 1 if parameter c is a digit
 * @c: input character
 * Return: 1 if c is a digit
 */

int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
