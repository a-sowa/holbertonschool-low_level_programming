#include "main.h"

/**
 * add - Entry point
 * add func: adds  two integer and return the value of sum
 * @nb1: input integer
 * @nb2: input integer
 * Return: sum value (Success)
 */

int add(int nb1, int nb2)
{
	int sum = nb1 + nb2;

	return (sum);
}
