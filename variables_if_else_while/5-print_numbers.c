#include <stdio.h>

/**
 * main - Entry point
 * Main function: prints digit number of base 10 starting from 0
 * Return: Always 0 (Success)
 */

int main(void)
{
	int nb = 0;

	while (nb < 10)
	{
	printf("%d", nb);
	nb++;
	}
	printf("\n");

	return (0);
}
